# Demo

D1 has only an install and import smoke demo.

```bash
python -m pip install -e .
python -c "import agent_coach; print(agent_coach.__version__)"
```

The interactive deterministic demo is planned for later slices after contracts,
Agent Core, mock adapters and the local Mock Agent API exist.
