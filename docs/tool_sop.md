# Tool SOP

Generated from the frozen public `ToolSpec` declarations used by the mock, local-vector and live-provider diploma profiles.

| Tool | Effect | When To Use | When Not To Use | Model Args | Trusted Context | Validation | Timeout/Retry | Expected Result | Errors/Security |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| cards.get_due | read-only | Use when the learner should review due cards, or to understand what material needs reinforcement. | Do not use as a write or scheduling action. | `deck_id` integer or null optional; `limit` integer optional; `tags` string or null optional. | Harness supplies user_id, session_id, run_id, question and adapter profile; model args cannot set identity. | Reject unknown args; reject harness identity fields. | Declared ToolSpec limits: max_result_chars=2000. Runner budgets still apply; retry policy is not declared in ToolSpec. | Bounded read-only result for the declared contract: Return flashcards due for spaced-repetition review right now, optionally filtered by deck or tags. | ToolSpec declares access=read-only and not idempotent. Error categories and retry semantics are not declared in ToolSpec; core validation rejects malformed or harness-owned args, and security projection redacts raw secrets, private paths and prompt-injection text. |
| catalog.list | read-only | Use to find where a topic lives in a course or konspekt section («найди раздел про X в Deep»), or to list courses without activating scope. | Do not use to browse private or external catalogs. | `course` string or null optional; `level` string or null optional; `query` string or null optional. | Harness supplies user_id, session_id, run_id, question and adapter profile; model args cannot set identity. | Reject unknown args; reject harness identity fields. | Declared ToolSpec limits: max_result_chars=6000. Runner budgets still apply; retry policy is not declared in ToolSpec. | Bounded read-only result for the declared contract: Browse the area library: courses, vault konspekts (type:konspekt), and section headings. Returns real relative paths only — never invent paths. Use course filter for a specific course folder (e.g. Deep). | ToolSpec declares access=read-only and not idempotent. Error categories and retry semantics are not declared in ToolSpec; core validation rejects malformed or harness-owned args, and security projection redacts raw secrets, private paths and prompt-injection text. |
| learner.get_profile | read-only | Use at the start of a study session or when personalization affects the answer depth, style, or next step. | Do not use to request private learner identifiers or credentials. | No model-selectable args. | Harness supplies user_id, session_id, run_id, question and adapter profile; model args cannot set identity. | Reject unknown args; reject harness identity fields. | Declared ToolSpec limits: max_result_chars=3000. Runner budgets still apply; retry policy is not declared in ToolSpec. | Bounded read-only result for the declared contract: Return the personalized learner model: mastery level, learning goal, preferred style, cognitive load, emotional state, and mastery vector. | ToolSpec declares access=read-only and not idempotent. Error categories and retry semantics are not declared in ToolSpec; core validation rejects malformed or harness-owned args, and security projection redacts raw secrets, private paths and prompt-injection text. |
| quiz.generate | read-only | Use to create a practice quiz for a topic the learner is studying, or to check understanding after an explanation. | Do not use before grounded topic evidence is available. | `learning_mode` string or null optional; `topic` string required. | Harness supplies user_id, session_id, run_id, question and adapter profile; model args cannot set identity. | Reject unknown args; reject harness identity fields. | Declared ToolSpec limits: max_result_chars=4000. Runner budgets still apply; retry policy is not declared in ToolSpec. | Bounded read-only result for the declared contract: Generate a scoped quiz (multiple-choice questions) for a topic from the knowledge base, without recording results. | ToolSpec declares access=read-only and not idempotent. Error categories and retry semantics are not declared in ToolSpec; core validation rejects malformed or harness-owned args, and security projection redacts raw secrets, private paths and prompt-injection text. |
| rag.search | read-only | Use when you need to find relevant source material, facts, or excerpts to ground your answer. Prefer this before answering factual questions. | Do not use for questions outside the packaged public corpus. | `query` string required; `top_k` integer optional. | Harness supplies user_id, session_id, run_id, question and adapter profile; model args cannot set identity. | Reject unknown args; reject harness identity fields. | Declared ToolSpec limits: max_result_chars=7200. Runner budgets still apply; retry policy is not declared in ToolSpec. | Bounded read-only result for the declared contract: Semantic search over the indexed knowledge base. Returns ranked source chunks (text excerpts) without generating an answer. | ToolSpec declares access=read-only and not idempotent. Error categories and retry semantics are not declared in ToolSpec; core validation rejects malformed or harness-owned args, and security projection redacts raw secrets, private paths and prompt-injection text. |
