# D1 requires no environment variables.
# Future slices must keep local demo defaults deterministic and safe.
#
# Optional live-provider profile (D9). Do not commit values.
# AGENT_COACH_LIVE_API_KEY=
# AGENT_COACH_PLANNER_MODEL=gpt-4.1-mini
# AGENT_COACH_SYNTHESIZER_MODEL=gpt-4.1
# AGENT_COACH_LIVE_API_BASE=https://api.openai.com/v1
# AGENT_COACH_LIVE_MAX_QUESTION_CHARS=2000
# AGENT_COACH_LIVE_MAX_RUN_TOKENS=4000
# AGENT_COACH_LIVE_RUN_TIME_LIMIT_SEC=60
